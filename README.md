# 📰 뉴스 검색 비교 & 저장 앱

LLM 기반 검색(Google Gemini + Search Grounding)과 키워드 기반 검색(네이버 뉴스 API)을 동시에 활용하여 동일 키워드의 검색 결과를 비교하고, 모든 결과를 Supabase 클라우드 데이터베이스에 자동 저장하는 Streamlit 웹 애플리케이션입니다.

비즈니스머신러닝 W9-10 실습과제2 결과물입니다.

## 🔗 링크

- **사이트 주소**: https://news-search-compare-fl3mfoqdagqsjn3wlx8xfy.streamlit.app/
- **GitHub 저장소**: [https://github.com/YOUR-USERNAME/YOUR-REPO](https://github.com/respect0098-prog/news-search-compare)

---

## 🛠️ 사용 기술 스택

| 구분 | 기술 |
|---|---|
| Frontend | Streamlit |
| LLM | Google Gemini (`gemini-2.5-flash-lite`) + Search Grounding |
| 외부 API | 네이버 검색 API (`/v1/search/news.json`) |
| Database | Supabase (PostgreSQL) |
| Hosting | Streamlit Community Cloud |
| Source Control | GitHub |

---

## 📊 데이터베이스 스키마

검색 소스별로 결과를 분리 저장하기 위해 두 개의 테이블을 사용합니다.

### `news_history` — 구글(Gemini) 검색 결과

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | BIGINT | PK, 자동 증가 |
| `keyword` | TEXT | 사용자 입력 키워드 |
| `title` | TEXT | 기사 제목 |
| `source` | TEXT | 언론사 이름 (Gemini가 본문에서 식별) |
| `news_date` | TEXT | 기사 발행일 (YYYY-MM-DD) |
| `url` | TEXT **UNIQUE** | 기사 원본 URL — 중복 저장 방지 키 |
| `summary` | TEXT | LLM이 생성한 3~4문장 요약 |
| `created_at` | TIMESTAMPTZ | DB 저장 시각 (자동) |

### `naver_news_history` — 네이버 검색 결과

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `id` | BIGINT | PK, 자동 증가 |
| `keyword` | TEXT | 사용자 입력 키워드 |
| `title` | TEXT | HTML 태그 제거된 기사 제목 |
| `description` | TEXT | HTML 태그 제거된 본문 일부 |
| `link` | TEXT **UNIQUE** | 네이버 뉴스 링크 — 중복 저장 방지 키 |
| `original_link` | TEXT | 원본 언론사 URL |
| `pub_date` | TEXT | 발행일 (RFC 822 형식) |
| `created_at` | TIMESTAMPTZ | DB 저장 시각 (자동) |

### 테이블 생성 SQL

Supabase SQL Editor에서 아래 쿼리 실행:

```sql
CREATE TABLE news_history (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    keyword TEXT NOT NULL,
    title TEXT NOT NULL,
    source TEXT,
    news_date TEXT,
    url TEXT UNIQUE NOT NULL,
    summary TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

CREATE TABLE naver_news_history (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    keyword TEXT NOT NULL,
    title TEXT NOT NULL,
    description TEXT,
    link TEXT UNIQUE NOT NULL,
    original_link TEXT,
    pub_date TEXT,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

---

## 🏗️ 코드 구조

`app.py`는 5개의 탭으로 구성되며, 각 탭의 동작 원리는 다음과 같습니다.

### 탭1. 🔍 구글 검색
- `search_google_news()` 호출
- `gemini-2.5-flash-lite` 모델에 **Google Search Grounding** 도구를 활성화한 채로 프롬프트 전송
- LLM이 검색 → 요약 → JSON 포맷팅까지 한 번에 수행
- ⚠️ Search Grounding과 강제 JSON(`response_mime_type`) 기능은 동시 사용 불가하므로, 프롬프트에 JSON 출력을 강하게 명시한 뒤 정규표현식으로 JSON 배열만 추출
- `save_google_news()`로 `news_history` 테이블에 자동 저장 (`url` UNIQUE 제약으로 중복 방지)

### 탭2. 🟢 네이버 검색
- `search_naver_news()` 호출
- 네이버 검색 API에 `requests.get()`으로 HTTP 요청
  - 헤더: `X-Naver-Client-Id`, `X-Naver-Client-Secret`
  - 파라미터: `query`, `display=5`, `sort=date`
- 응답의 `title`, `description` 필드는 검색어 강조용 `<b>` 태그가 포함되어 있어 `clean_html()` 함수로 정제
  - `re.sub(r'<[^>]+>', '', text)` — HTML 태그 제거
  - `html.unescape(text)` — `&quot;`, `&amp;` 같은 엔티티를 일반 문자로 변환
- `save_naver_news()`로 `naver_news_history` 테이블에 자동 저장 (`link` UNIQUE 제약으로 중복 방지)

### 탭3. ⚖️ 비교 검색
- 동일 키워드로 두 검색 함수를 순차 호출
- `st.columns(2)`로 좌우 분할하여 결과를 나란히 표시
- 하단에 정량 비교 메트릭(건수, URL 중복 건수)과 정성 비교 인사이트 자동 표시

### 탭4. 💾 저장된 뉴스 보기
- 라디오 버튼으로 `news_history` / `naver_news_history` 테이블 선택
- 키워드 필터링 + `st.dataframe`으로 표 출력
- CSV 다운로드 버튼 제공 (한글 깨짐 방지: `utf-8-sig` 인코딩)

### 탭5. 📊 통계 분석
- 두 테이블을 메모리에서 병합한 후 다음 차트 생성:
  - 소스별(구글/네이버) 누적 건수
  - 키워드별 누적 검색 건수 (Top 10)
  - 일자별 저장 건수 (소스별 스택)
- 상단에 4개 메트릭 카드(구글/네이버 누적, 키워드 종류, 총 건수)

### 주요 헬퍼 함수

| 함수 | 역할 |
|---|---|
| `init_supabase()` | `@st.cache_resource`로 캐싱된 Supabase 클라이언트 |
| `clean_html(text)` | HTML 태그 + 엔티티 제거 |
| `search_google_news(keyword)` | Gemini + Grounding으로 뉴스 검색·요약 |
| `search_naver_news(keyword)` | 네이버 검색 API 호출 |
| `save_google_news(kw, list)` | `news_history`에 INSERT (중복 무시) |
| `save_naver_news(kw, list)` | `naver_news_history`에 INSERT (중복 무시) |

### Session State

다운로드 버튼 클릭이나 다른 탭 이동 시에도 검색 결과가 사라지지 않도록 `st.session_state`에 다음 키를 보관:
- `google_results`, `naver_results`, `compare_google`, `compare_naver`

---

## 🆚 구글(Gemini) vs 네이버 검색 방식 비교

| 측면 | 구글 (Gemini + Grounding) | 네이버 검색 API |
|---|---|---|
| 검색 방식 | 의미·맥락 기반 (semantic) | 키워드 매칭 기반 (lexical) |
| 결과 정렬 | LLM의 관련성 판단 | 발행일 또는 정확도 |
| 결과 가공 | LLM 자동 요약 (3~4문장) | 본문 일부(description)만 |
| 인덱싱 범위 | 전 세계 웹 (블로그·영문 매체 포함) | 네이버 뉴스 등록 한국 매체 |
| 다국어 지원 | 강력 (자동 번역·통합) | 한국어 중심 |
| 호출 한도 | 분당 15회 / 일 1,500회 (무료) | 일 25,000회 (무료) |
| 응답 시간 | 5~15초 (LLM 추론) | < 1초 |
| 비용 | LLM 토큰 비례 | 무료 |

**언제 무엇을 쓸까:**
- 빠르게 한국 언론사 최신 기사 목록이 필요하면 → **네이버**
- 다양한 출처를 종합한 의미 기반 요약이 필요하면 → **구글(Gemini)**
- 영문 매체나 글로벌 트렌드도 함께 봐야 하면 → **구글(Gemini)**

---

## 🔐 환경 변수 설정

본 앱은 **5개의 비밀 키**를 사용하며, Streamlit Community Cloud의 **Secrets** 섹션에 다음 형식(TOML)으로 등록합니다.

```toml
GEMINI_API_KEY = "AIza..."
SUPABASE_URL = "https://xxxxxxxx.supabase.co"
SUPABASE_KEY = "sb_publishable_..."
NAVER_CLIENT_ID = "..."
NAVER_CLIENT_SECRET = "..."
```

### 키 발급처

| 키 | 발급처 |
|---|---|
| `GEMINI_API_KEY` | [Google AI Studio → Get API key](https://aistudio.google.com/api-keys) |
| `SUPABASE_URL` / `SUPABASE_KEY` | Supabase 프로젝트 → Connect → Project URL & Publishable key |
| `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` | [네이버 개발자 센터 → 애플리케이션 등록](https://developers.naver.com/apps/) (검색 API 선택) |

---

## 🚀 로컬 실행 (선택 사항)

```bash
# 1. 저장소 클론
git clone https://github.com/YOUR-USERNAME/YOUR-REPO.git
cd YOUR-REPO

# 2. 의존성 설치
pip install -r requirements.txt

# 3. 비밀 키 설정 — .streamlit/secrets.toml 파일 생성 후 위의 TOML 형식으로 입력
mkdir -p .streamlit
# (편집기로 .streamlit/secrets.toml 작성)

# 4. 실행
streamlit run app.py
```

> ⚠️ `.streamlit/secrets.toml`은 절대 GitHub에 커밋하지 마세요. `.gitignore`에 추가 권장.

## 🌐 Streamlit Community Cloud 배포

1. <https://share.streamlit.io> 접속 → GitHub 계정으로 로그인
2. **Create app → Deploy a public app from GitHub**
3. 입력:
   - Repository: 본인 저장소
   - Branch: `main`
   - Main file path: `app.py`
4. **Advanced settings → Secrets**에 위 TOML 형식의 5개 키 입력
5. **Deploy** 클릭

---

## ⚠️ 주의 사항

- **Gemini 무료 티어**: 분당 15회 / 일 1,500회 제한. 검색 버튼을 빠르게 여러 번 누르면 429 에러 발생.
- **네이버 검색 API**: 일 25,000회 한도. 사용량이 비정상적으로 많으면 IP 차단 가능.
- **Supabase 무료 티어**: DB 500MB / Storage 1GB / 사용자 5만 명·월.
- API 키와 비밀번호는 절대 코드에 직접 포함시키지 말고 반드시 `st.secrets` 사용.
- Gemini가 만든 요약은 사실과 다를 수 있으므로 원본 기사 링크로 검증 필요.

---

## 📄 라이선스

학습 및 학술 용도로 자유롭게 사용 가능합니다.
